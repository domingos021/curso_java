package jpa_hibernate;

/*
 * Setup Steps
 * ===========
 *
 * 1) Create an empty MySQL database
 * ---------------------------------
 * - Install XAMPP on your computer.
 *   XAMPP is a package that provides a local development environment,
 *   including Apache (web server) and MySQL (database server).
 *
 * - Start the Apache and MySQL services.
 *
 * - Open phpMyAdmin (included with XAMPP) and create a new database
 *   named "aulajpa".
 *
 * 2) Create a new Maven project
 * -----------------------------
 * - File -> New -> Other -> Maven Project
 * - Select "Create a Simple Project" -> Next
 * - Group Id: com.educandoweb
 * - Artifact Id: aulajpamaven
 * - Click Finish.
 *
 * 3) Copy the classes
 * -------------------
 * - Copy the Program and Person classes into the new project.
 *
 * 4) Update the Maven project
 * ---------------------------
 * - Edit the pom.xml file.
 * - Configure the project to use the current Java LTS version.
 * - Save the file.
 * - Right-click the project -> Maven -> Update Project
 *   (check "Force Update" if necessary).
 */


//http://localhost/phpmyadmin
public class Jpa_first_example {
}
