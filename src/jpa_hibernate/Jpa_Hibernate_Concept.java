package jpa_hibernate;
/*
 * JPA (Java Persistence API)
 * --------------------------
 * JPA is a specification that defines how Java objects are mapped
 * to relational databases.
 *
 * Hibernate
 * ---------
 * Hibernate is the most popular implementation of the JPA specification.
 *
 * Relationship:
 * - JPA defines the rules.
 * - Hibernate implements those rules.
 *
 * Example:
 * JPA -> Interface
 * Hibernate -> Implementation
 *
 * Main JPA classes
 * ----------------
 * 1. EntityManager:
 *    Responsible for managing the lifecycle of entities
 *    (Java objects that represent database tables).
 *
 * 2. EntityManagerFactory:
 *    Responsible for creating EntityManager instances.
 *
 * Scope
 * -----
 * The entire application needs only one EntityManagerFactory instance.
 * This factory is used to create multiple EntityManager objects whenever
 * they are needed.
 */
public class Jpa_Hibernate_Concept {
    public static void main(String[] args) {

    }

}