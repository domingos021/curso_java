package interfaces.model.services;

//Generica ou contrato
public interface TaxService {
    //define que toda classe que implementar essa interface deve ter o método tax
    //ou sej ade
    double tax(double amount);
}
